[project]
name = "cot-pl0-compiler"
version = "0.1.0"
description = "A toy compiler from PL/0 to Aarch64"
readme = "README.md"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "black>=26.3.1",
    "ruff>=0.15.12",
]

[tool.black]
line-length = 80
target-version = ['py310']
